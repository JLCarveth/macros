# Database
DATABASE_URL=postgresql://user:password@localhost:5432/nutrition_llama

# JWT Secrets (generate with: openssl rand -hex 32)
ACCESS_TOKEN_SECRET=your-access-token-secret-here
REFRESH_TOKEN_SECRET=your-refresh-token-secret-here

# API Configuration
NUTRITION_API_URL=http://localhost:3000

# Server Ports
PORT=8000       # Frontend (Fresh) - defaults to 8000
API_PORT=3000   # Backend API - defaults to 3000
# NODE_ENV=development  # Set to "development" for HTTPS
# SSL_KEY_PATH=/path/to/localhost-key.pem
# SSL_CERT_PATH=/path/to/localhost.pem
# LOG_TIMINGS=true  # Enable performance logging

# Plausible Analytics (optional - omit to disable)
# PLAUSIBLE_URL=https://plausible.example.com
# PLAUSIBLE_DOMAIN=eater.example.com
# PLAUSIBLE_SCRIPT_PATH=/js/script.js
